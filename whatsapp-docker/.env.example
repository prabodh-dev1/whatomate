# Server timezone (used for IVR timing node business hours)
TZ=Asia/Kolkata

# PostgreSQL (host access: localhost:7432 when using default compose ports)
POSTGRES_USER=whatomate
POSTGRES_PASSWORD=change-me
POSTGRES_DB=whatomate

# Public URL for Meta WhatsApp webhooks (configure in Meta Developer Console)
# WHATOMATE_PUBLIC_URL=https://crm.prabodh.in
